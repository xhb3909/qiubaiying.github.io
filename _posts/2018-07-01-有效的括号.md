---
layout:     post
title:      有效的括号
subtitle:   leetcode第20题
date:       2018-07-01
author:     hoabo
header-img: img/post-bg-kuaidi.jpg
catalog: true
tags:
    - leetcode
---

# 题目说明

给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串，判断字符串是否有效。
有效字符串需满足：
  1. 左括号必须用相同类型的右括号闭合。
  2. 左括号必须以正确的顺序闭合。
注意空字符串可被认为是有效字符串。

# java实现代码(含注释)
```java
public class Solution {

    /**
     * 判断元素是否合法
     * @param s
     * @return
     */
    public boolean isValid(String s) {
        Stack<Character> stack = new Stack<Character>();
        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            // 判断当前字符是否是'(', '[', '{' 如果成立直接push进去
            if (c == '(' || c == '[' || c == '{')
                stack.push(c);
            else {
                // 如果当前栈中的元素为空,则直接返回false
                if (stack.isEmpty())
                    return false;
                // 弹出栈中的元素
                char topChar = stack.pop();
                // ')' ']' '}' 和 '(' '[' '{' 是否匹配
                if (c == ')' && topChar != '(')
                    return false;
                if (c == ']' && topChar != '[')
                    return false;
                if (c == '}' && topChar != '{')
                    return false;
            }
        }

        // 最后还要确定栈中的元素是否为空
        return stack.isEmpty();
    }
}



```

